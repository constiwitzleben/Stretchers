# Stretcher — pip dependencies (partial environment)
#
# This file covers everything EXCEPT FEniCS, which is distributed through
# conda-forge only and cannot be pip-installed. With these packages you can run:
#
#   - real_matching.ipynb      (matching on real deformed surgical images)
#   - dataset_creation.ipynb   (building the descriptor-pair dataset)
#   - model_training.ipynb     (training the Stretcher network)
#
# The FEM-based synthetic evaluation (synthetic_matching.ipynb and
# scripts/evaluate_table1.py, which reproduces Table 1 of the paper) additionally
# requires FEniCS 2019.1.0. For that, use the conda environment instead:
#
#   conda env create -f environment.yml
#   conda activate stretcher
#
# For CUDA builds of torch, install from https://pytorch.org after this file.

# --- deep learning ---
torch==2.6.0
torchvision==0.21.0
kornia==0.8.0
einops==0.8.1

# --- imaging / geometry ---
numpy==2.0.2
scipy==1.13.1
scikit-image==0.24.0
opencv-python==4.11.0.86
pillow==11.1.0
imageio==2.37.0

# --- mesh rendering (used by the FEM pipeline; harmless otherwise) ---
pyvista==0.44.2
vtk==9.3.1

# --- plotting / logging / notebooks ---
matplotlib==3.9.4
tensorboard==2.20.0
ipykernel==6.30.1
